/**
 * # Firestore Security Rules: CareerPilot AI
 *
 * ## Core Philosophy
 * This ruleset enforces a strict user-ownership model. All user-generated data is
 * private and can only be accessed by the user who created it. There is no
 * public or shared data. The security model defaults to denying all access,
 * only granting specific permissions based on verified user identity.
 *
 * ## Data Structure
 * All data is stored hierarchically under the `/users/{userId}` collection. This
 * structure ensures that every piece of data has a clear owner, determined by
 * the `userId` in the document path. This design simplifies security logic and
 * makes list operations inherently secure, as queries are naturally scoped to
 * the signed-in user's data tree.
 *
 * ## Key Security Decisions
 * - **Strict Ownership**: All rules verify that the `request.auth.uid` matches
 *   the `userId` in the document path. A user can never see or modify another
 *   user's data.
 * - **No User Enumeration**: Listing documents in the top-level `/users`
 *   collection is explicitly forbidden to protect user privacy.
 * - **Relational Integrity**: On creation, rules validate that internal ID
 *   fields (like `userId` or `interviewSessionId`) match the corresponding IDs
 *   in the document path. These fields are then enforced as immutable on updates
 *   to prevent documents from being moved or re-associated.
 * - **Prototyping Flexibility**: While authorization and relational integrity
 *   are strictly enforced, these rules do not validate the specific shape or
 *   data types of document content (e.g., `resumeText`, `atsScore`). This
 *   allows for rapid iteration on the application's data models without
 *   requiring rule changes.
 *
 * ## Denormalization for Authorization
 * The security model relies on the path structure itself for authorization, which is the
 * most performant approach. For relational integrity, key identifiers (like `userId`) are
 * denormalized onto child documents, allowing for simple and fast validation checks
 * without needing costly `get()` or `exists()` calls to other documents.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // =====================================================================
    // Helper Functions
    // =====================================================================

    /**
     * Checks if a user is authenticated.
     */
    function isSignedIn() {
      return request.auth != null;
    }

    /**
     * Checks if the currently authenticated user's ID matches the
     * userId parameter from the document path.
     */
    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    /**
     * Verifies ownership for an existing document. Used for update/delete
     * operations to ensure the document exists before the operation proceeds.
     */
    function isExistingOwner(userId) {
      return isOwner(userId) && resource != null;
    }

    // --- Data Integrity Validation Functions ---

    /**
     * CREATE: Validates the user document's internal `id` field matches the
     * user's own UID, establishing the ownership link within the data.
     */
    function userDocumentHasCorrectIdOnCreate(userId) {
      return request.resource.data.id == userId;
    }

    /**
     * UPDATE: Enforces that the user document's internal `id` field is immutable.
     */
    function isUserDocumentIdImmutable() {
      return request.resource.data.id == resource.data.id;
    }

    /**
     * CREATE: Validates a sub-collection document's `userId` field matches
     * the owner's ID from the path.
     */
    function hasCorrectUserIdOnCreate(userId) {
      return request.resource.data.userId == userId;
    }

    /**
     * UPDATE: Enforces that a sub-collection document's `userId` field is immutable.
     */
    function isUserIdImmutable() {
      return request.resource.data.userId == resource.data.userId;
    }

    /**
     * CREATE: Validates an InterviewQuestion's `interviewSessionId` field
     * matches the parent document's ID from the path.
     */
    function hasCorrectInterviewSessionIdOnCreate(interviewSessionId) {
      return request.resource.data.interviewSessionId == interviewSessionId;
    }

    /**
     * UPDATE: Enforces that an InterviewQuestion's `interviewSessionId` is immutable.
     */
    function isInterviewSessionIdImmutable() {
      return request.resource.data.interviewSessionId == resource.data.interviewSessionId;
    }


    // =====================================================================
    // Collection Rules
    // =====================================================================

    /**
     * @description Controls access to a user's own profile document.
     * @path /users/{userId}
     * @allow A signed-in user (auth.uid='user123') can (create) their own profile at /users/user123.
     * @deny A signed-in user (auth.uid='user456') cannot (get) or (update) the profile at /users/user123.
     * @principle Restricts access to a user's own data tree and prevents user enumeration.
     */
    match /users/{userId} {
      allow get: if isOwner(userId);
      allow list: if false;
      allow create: if isOwner(userId) && userDocumentHasCorrectIdOnCreate(userId);
      allow update: if isExistingOwner(userId) && isUserDocumentIdImmutable();
      allow delete: if isExistingOwner(userId);

      /**
       * @description Controls access to a user's resume documents.
       * @path /users/{userId}/resumes/{resumeId}
       * @allow A signed-in user (auth.uid='user123') can (list) their own resumes at /users/user123/resumes.
       * @deny A signed-in user (auth.uid='user456') cannot (delete) a resume at /users/user123/resumes/resumeABC.
       * @principle Enforces strict ownership for all operations on a user's private data.
       */
      match /resumes/{resumeId} {
        allow get: if isOwner(userId);
        allow list: if isOwner(userId);
        allow create: if isOwner(userId) && hasCorrectUserIdOnCreate(userId);
        allow update: if isExistingOwner(userId) && isUserIdImmutable();
        allow delete: if isExistingOwner(userId);
      }

      /**
       * @description Controls access to a user's interview session documents.
       * @path /users/{userId}/interviewSessions/{interviewSessionId}
       * @allow A signed-in user (auth.uid='user123') can (create) a new session at /users/user123/interviewSessions/sessionABC.
       * @deny An anonymous user cannot (get) any interview session.
       * @principle Enforces strict ownership for all operations on a user's private data.
       */
      match /interviewSessions/{interviewSessionId} {
        allow get: if isOwner(userId);
        allow list: if isOwner(userId);
        allow create: if isOwner(userId) && hasCorrectUserIdOnCreate(userId);
        allow update: if isExistingOwner(userId) && isUserIdImmutable();
        allow delete: if isExistingOwner(userId);

        /**
         * @description Controls access to questions within a user's interview session.
         * @path /users/{userId}/interviewSessions/{interviewSessionId}/interviewQuestions/{interviewQuestionId}
         * @allow A signed-in user (auth.uid='user123') can (create) a question in their own session at /users/user123/interviewSessions/sessionABC/interviewQuestions/questionXYZ.
         * @deny An anonymous user cannot (get) any interview question.
         * @principle Inherits ownership from the parent user path and validates relational integrity with the parent session document.
         */
        match /interviewQuestions/{interviewQuestionId} {
          allow get: if isOwner(userId);
          allow list: if isOwner(userId);
          allow create: if isOwner(userId) && hasCorrectInterviewSessionIdOnCreate(interviewSessionId);
          allow update: if isExistingOwner(userId) && isInterviewSessionIdImmutable();
          allow delete: if isExistingOwner(userId);
        }
      }

      /**
       * @description Controls access to a user's skill gap analysis documents.
       * @path /users/{userId}/skillGapAnalyses/{skillGapAnalysisId}
       * @allow A signed-in user (auth.uid='user123') can (get) their own analysis at /users/user123/skillGapAnalyses/analysisABC.
       * @deny A signed-in user (auth.uid='user456') cannot (list) another user's analyses.
       * @principle Enforces strict ownership for all operations on a user's private data.
       */
      match /skillGapAnalyses/{skillGapAnalysisId} {
        allow get: if isOwner(userId);
        allow list: if isOwner(userId);
        allow create: if isOwner(userId) && hasCorrectUserIdOnCreate(userId);
        allow update: if isExistingOwner(userId) && isUserIdImmutable();
        allow delete: if isExistingOwner(userId);
      }

      /**
       * @description Controls access to a user's job description analysis documents.
       * @path /users/{userId}/jobDescriptionAnalyses/{jobDescriptionAnalysisId}
       * @allow A signed-in user (auth.uid='user123') can (delete) their own analysis at /users/user123/jobDescriptionAnalyses/analysisABC.
       * @deny A signed-in user (auth.uid='user456') cannot (create) an analysis for another user.
       * @principle Enforces strict ownership for all operations on a user's private data.
       */
      match /jobDescriptionAnalyses/{jobDescriptionAnalysisId} {
        allow get: if isOwner(userId);
        allow list: if isOwner(userId);
        allow create: if isOwner(userId) && hasCorrectUserIdOnCreate(userId);
        allow update: if isExistingOwner(userId) && isUserIdImmutable();
        allow delete: if isExistingOwner(userId);
      }
    }
  }
}